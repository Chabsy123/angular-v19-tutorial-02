export interface Iuser {
    id : number,
    name : string,
    username : string,
    email : string,
}
// This is a TypeScript interface.

// It ensures that whenever we handle user data, it must follow this structure.

// Used inside the CrudService and components for type safety.
